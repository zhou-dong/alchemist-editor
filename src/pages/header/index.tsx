import React from "react";

const style = {
    backgroundColor: "#073642",
    textAlign: "center",
} as React.CSSProperties;

const Header = () => (
    <header style={style}>
    ALCHEMIST HEADER
    </header>
);

export default Header;
