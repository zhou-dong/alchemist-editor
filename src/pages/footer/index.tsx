import React from 'react';

const style = {
    backgroundColor: "#073642",
    textAlign: "center",
} as React.CSSProperties;

const Footer = () => (
    <footer style={style}>
        ALCHEMIST EDITOR
        <br />
        FOOTER
    </footer>
);

export default Footer;
