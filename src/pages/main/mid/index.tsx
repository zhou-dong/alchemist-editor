import React from 'react';

const layoutStyle = {
    flex: 1,
    overflow: "hidden"
};

const style = {
    ...layoutStyle,
    backgroundColor: "#586e75",
};

export default () => (
    <section style={style} id="display">
        section
    </section>
);
